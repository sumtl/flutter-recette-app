import 'package:flutter/material.dart';

/// Application-wide constants.
/// Keep colors, text styles, spacing, and other reusable values here so they
/// can be referenced throughout the app for consistent design.
const Color kprimaryColor = Color(0xFF1E88E5); // Primary brand color (blue)
